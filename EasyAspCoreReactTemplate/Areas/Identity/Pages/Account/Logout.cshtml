@page
@model LogoutModel
@{
    ViewData["Title"] = "Log out";
}

<section class="max-w-xl rounded-3xl border border-slate-200/70 dark:border-slate-800/60 bg-white/80 dark:bg-slate-950/40 p-8">
    <h1 class="text-2xl font-semibold text-slate-900 dark:text-white">@ViewData["Title"]</h1>
    @{
        if (User.Identity?.IsAuthenticated ?? false)
        {
            <form class="mt-4" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                <button type="submit" class="rounded-full border border-slate-300/70 dark:border-slate-700/60 px-4 py-2 text-sm font-semibold text-slate-700 dark:text-slate-200 transition hover:border-slate-400/60 hover:text-slate-900 dark:hover:text-white">Click here to Logout</button>
            </form>
        }
        else
        {
            <p class="mt-4 text-sm text-slate-600 dark:text-slate-300">You have successfully logged out of the application.</p>
        }
    }
</section>




